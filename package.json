{
  "name": "random-game-generator",
  "version": "0.1.0",
  "private": true,
  "description": "Gestor de backlog gamer con selección aleatoria inteligente. Extensión Chrome MV3.",
  "type": "module",
  "scripts": {
    "dev": "extension dev",
    "start": "extension start",
    "build": "extension build",
    "typecheck": "tsc --noEmit && tsc -p server",
    "test": "node --import tsx --test tests/*.test.ts",
    "api:dev": "tsx server/dev.ts",
    "api:typecheck": "tsc -p server"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.20.2",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "extension": "^2.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.3"
  }
}
